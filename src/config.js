const config = {
    base: {
        slug: '/pages/',
        file: '.dgm'
    },
    options: {
        page: {
            pageName: 'Hybrid App',
            isDrawerOpen: false,
        },
    }, 
};

export default config;