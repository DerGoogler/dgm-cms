const config = {
    base: {
        slug: '/pages/',
        file: '.dgm',
    },
    options: {
        drawer: {
            isDrawerOpen: false,
            width: 250, // don't make it over 500!
            collapse: true,
            swipeable: true
        }
    }, 
};

export default config;