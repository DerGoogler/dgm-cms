const config = {
    base: {
        slug: '/pages/',
        file: '.dgm',
    },
    options: {
        page: {
            pageTitle: 'Hybrid App',
            isDrawerOpen: false,
        }
    }, 
};

export default config;